/**
 * Drizzle schema — the single source of truth for the database.
 *
 * Epic 1 fills this in: `sponsors`, `resumes`, `jobs` (with pgvector embedding),
 * `job_scores`, `applications`, `contacts`, `outreach_log`. See CLAUDE.md → Data
 * model, and the `db-change` skill for the migration workflow.
 *
 * Intentionally empty during Phase 0 scaffolding.
 */
export {};
